baseURL: "https://hugo-profile.netlify.app"
languageCode: "en-us"
title: "Daniel de Gouw"
theme: hugo-profile

outputs:
  home:
    - "HTML"
    - "RSS"
    - "JSON"
  page:
    - "HTML"
    - "RSS"

enableRobotsTXT: true

pagination:
  pagerSize: 3

markup:
  goldmark:
    renderer:
      unsafe: true

menu:
  main:
    - name: "Portfolio"
      url: "/portfolio/"
      weight: 1

params:
  title: "Daniel de Gouw"
  description: Text about my cool site
  # staticPath: ""  # The path to serve the static files from
  favicon: "/fav.png"

  
  socialLinks:
    fontAwesomeIcons:
      github:
        icon: fab fa-github
        url: https://example.com
      twitter:
        icon: fab fa-x-twitter
        url: https://example.com
    customIcons:
      website:
        icon: /fav.png
        url: https://example.com

  # Whether to serve bootstrap css and js files from CDN or not. Can be set to true, "css" or "js" to choose between
  # serving both, only the css, or only the js files through the CDN. Any other value will make so that CDN is not used.
  # Note the lack of "" in true, it should be of boolean type.
  useBootstrapCDN: true

  # If you want to load dynamically responsive images from Cloudinary
  # This requires your images to be uploaded + hosted on Cloudinary
  # Uncomment and change YOUR_CLOUD_NAME to the Cloud Name in your Cloudinary console
  # cloudinary_cloud_name: "YOUR_CLOUD_NAME"

  # Whether to add mathjax support on all pages. Alternatively, you can opt-in per page by adding `mathjax: true` in the frontmatter.
  mathjax: false

  # Whether the fade animations on the home page will be enabled
  animate: true
  

  theme:
    # disableThemeToggle: true
    # defaultTheme: "light" # dark

  font:
    fontSize: 1rem # default: 1rem
    fontWeight: 400 # default: 400
    lineHeight: 1.5 # default: 1.5
    textAlign: left # default: left

  # color preference
  color:
    textColor: "#000000" #"#6c757d" # Also applies to page nav
    secondaryTextColor: "#6c757d"
    headingsTextColor: "#343a40"
    subtitleTextColor: "#6b7280"
    contentTextColor: "#6c757d"
    textLinkColor: "#fc6f5c"
    backgroundColor: "#eaedf0"
    secondaryBackgroundColor: "#64ffda1a"
    primaryColor: "#fc6f5c" 
    secondaryColor: "#f8f9fa"
    tagText: "#4338ca"
    tagBackground: "#e0e7ff"
    tagBackgroundHover: "#c7d2fe"
    projectCardBackground: "#ffffff"
    tagFilterBackground: "#eef2ff"
    tagFilterBackgroundHover: "#dbeafe"
    tagFilterBackgroundActive: "#4f46e5"
    tagFilterText: "#4338ca"

    darkmode:
      textColor: "#e4e6eb"
      secondaryTextColor: "#b0b3b8"
      headingsTextColor: "#b0b3b8"
      subtitleTextColor: "#b0b3b8"
      contentTextColor: "#b0b3b8"
      textLinkColor: "#ffffff"
      backgroundColor: "#18191a"
      secondaryBackgroundColor: "#212529"
      primaryColor: "#ffffff"
      secondaryColor: "#212529"
      tagBackground: "#374151"
      tagText: "#e0e7ff"
      tagBackgroundHover: "#4b5565"
      projectCardBackground: "#212529" # 1f2937 as backup color
      tagFilterBackground: "#1f2937"
      tagFilterBackgroundHover: "#374151"
      tagFilterBackgroundActive: "#6366f1"
      tagFilterText: "#e0e7ff"
  
  search:
    showDescriptions: true

  # If you want to customize the menu, you can change it here
  navbar:
    align: ms-auto # Left: ms-auto | center: mx-auto | right: me-auto | Default: ms-auto
    # brandLogo: "/logo.png" # Logo for the brand | default is the favicon variable
    showBrandLogo: false # Show brand logo in nav bar | default is true
    # brandName: "Daniel de Gouw" # Brand name for the brand | default is the title variable
    disableSearch: false
    searchPlaceholder: "Search..."
    stickyNavBar:
      enable : true
      showOnScrollUp : false
    menus:
      disableAbout: false
      disableExperience: false
      disableEducation: false
      disableTechstack: false
      disableAchievements: false
      disableContact: false

  # Hero
  hero:
    enable: true

  # About
  about:
    enable: true

  # Experience
  experience:
    enable: true
    # title: "Custom Name"

  # Education
  education:
    enable: true
    # title: "Custom Name"

  # Achievements
  achievements:
    enable: true
    # title: "Custom Name"

  # Tech Stack
  techstack:
    enable: true
    # title: "Custom Name"

  #Contact
  contact:
    enable: true
    # title: "Custom Name"

  footer:
    publicationDate: 2026
    copyrightName: "Daniel de Gouw"
    socialLinks:
      fontAwesomeIcons:
        - github
        - twitter
      customIcons:
        # - website
        
  # List pages like blogs and posts
  listPages:
    disableFeaturedImage: false

  # Single pages like blog and post
  singlePages:
    socialShare: true
    readTime:
      enable: true
      content: "min read"
    scrollprogress:
      enable: true
    tags:
      openInNewTab: false

  # For translations
  terms:
    read: "Read"
    toc: "Table Of Contents"
    copyright: "All rights reserved"
    pageNotFound: "Page not found"
    emailText: "Check out this site"

  datesFormat:
    article: "Jan 2, 2006"
    articleList: "Jan 2, 2006"
    articleRecent: "Jan 2, 2006"

  #customScripts: -| # You can add custom scripts which will be added before </body> tag
  #  <script type="text/javascript"><!-- any script here --></script>

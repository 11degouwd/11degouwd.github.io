{
  "name": "portfolio-site-tests",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:update-snapshots": "playwright test --update-snapshots",
    "test:lighthouse": "lhci autorun --collect.url=http://localhost:1313"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@lhci/cli": "^0.14.0"
  }
}

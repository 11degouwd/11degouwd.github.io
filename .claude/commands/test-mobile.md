---
description: Run the full cross-device Playwright test pass against the local Hugo server and report results
---

Use the qa-tester subagent to run a full test pass on the currently checked
out branch: desktop, iPhone 15, and Pixel 8 viewports, console error check,
and broken link/image check. If $ARGUMENTS is provided, focus the manual pass
on that specific feature/page in addition to the full suite.
